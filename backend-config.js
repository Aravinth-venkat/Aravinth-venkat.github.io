/* Public frontend configuration — do not put secrets here. */
window.ARAVINTH_CONFIG = {
  backendUrl: "", // Paste your deployed Google Apps Script /exec URL here.
  siteUrl: "https://aravinth-venkat.github.io",
  maxResumeBytes: 5 * 1024 * 1024
};
